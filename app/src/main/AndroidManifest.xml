<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unsa.epis.sensores_laboratorio_07">
    <uses-feature android:name="android.hardware.Sensor.acelerometro"/>
    <uses-feature android:name="android.hardware.Sensor.magnetrometro"/>
    <uses-feature android:name="android.hardware.Sensor.gravedad"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sensores_Laboratorio_07">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>